# 🔧 快速修复参考

## ✅ 已修复的核心问题

### 1. 选择器错误 → 修复下载失败
```javascript
// 位置: content.js:1833
- container.querySelectorAll('div[data-result-index]')  ❌
+ container.querySelectorAll('div[data-index]')         ✅
```

### 2. 未定义变量 → 修复错误处理
```javascript
// 位置: content.js:391-395 (新增)
const currentTaskIndex = this.currentTaskPointer || (errorState ? errorState.currentIndex : 0);
```

### 3. Rate Limit → 增加延迟
```javascript
// 位置: content.js:29
TASK_DELAY: 20000,  // 12s → 20s
```

### 4. 状态检测 → 自动重试失败任务
```javascript
// 位置: content.js:1954-1983 (新增)
if (statusText.includes('Failed Generation')) {
  throw new RetryTaskError(...);
}
```

---

## 🎯 测试检查清单

- [ ] **重新加载扩展** (`chrome://extensions/` → 重新加载按钮)
- [ ] **刷新Flow页面**
- [ ] **开始批量任务** (3-6个任务)
- [ ] **查看日志**: 应该看到 `✅ 找到 X 个视频卡片（data-index）`
- [ ] **验证下载**: 视频应该自动下载到默认下载文件夹
- [ ] **验证重试**: 失败任务应该自动重试（最多3次）
- [ ] **验证延迟**: 任务之间间隔20秒

---

## 📊 预期日志输出

### ✅ 正常流程
```
✅ 找到 3 个视频卡片（data-index）
⏳ 任务 1 生成中: 15%
⏳ 任务 1 生成中: 50%
✅ 视频生成完成 (任务 1): https://storage.googleapis.com/...
📥 任务 1 开始下载...
✅ 任务 1 完成并已下载
```

### ⚠️ 失败重试
```
❌ 任务 3 生成失败，触发重试
⏸️ 任务 3 第 1 次重试 (剩余 2 次机会)
```

### ❌ 不应该看到
```
下载请求失败: 缺少下载 URL                    ❌
Queue processing error: currentTaskIndex is not defined  ❌
⚠️ 未检测到视频卡片，继续等待...                  ❌
```

---

## 🆘 如果仍有问题

1. **打开控制台** (F12)
2. **复制所有日志** (以 `[FlowBatchPilot]` 开头的所有内容)
3. **检查网络请求** (Network标签 → 查看429错误)
4. **提供页面HTML** (右键检查元素 → 复制外部HTML)
